src=src
src.main=${src}/main
src.main.resources=${src.main}/resources
src.webapp=${src.main}/webapp
src.webapp.webxml=${src.webapp}/WEB-INF/web.xml
src.test=${src}/test

build=build
build.doc=${build}/doc
build.classes=${build}/classes
build.tests=${build}/tests

dist=dist
dist.lib=${dist}/lib
dist.test=${dist}/tests
dist.war.name=${dist}/${ant.project.name}.war
dist.jar.name=${dist}/${ant.project.name}.jar
lib=lib

project.name=web3lab
project.version=1.0.0-ALPHA

ignored.classes=wcmwkcm

ivy.install.version=2.5.0
ivy.jar.dir=${basedir}/ivy
ivy.jar.file=${ivy.jar.dir}/ivy.jar
